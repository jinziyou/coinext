# Coinext — Rust workspace (the hot path + shared domain, the source of truth).
# Python mirrors this domain via PyO3 (see foundation/crates/coinext-py and root workflow module */python directories).
[workspace]
resolver = "2"
members = [
    "foundation/crates/coinext-core",
    "foundation/crates/coinext-model",
    "foundation/crates/coinext-ports",
    "foundation/crates/coinext-cache",
    "operations-interface/crates/coinext-bus",
    "strategy-research/crates/coinext-indicators",
    "analytics-optimization/crates/coinext-derivatives",
    "risk-portfolio/crates/coinext-risk-engine",
    "risk-portfolio/crates/coinext-portfolio",
    "market-data/crates/coinext-data-engine",
    "execution-live/crates/coinext-exec-engine",
    "backtesting-simulation/crates/coinext-sim",
    "backtesting-simulation/crates/coinext-kernel",
    "foundation/crates/coinext-testkit",
    "foundation/crates/coinext-py",
    "backtesting-simulation/examples/backtest-sma",
]
# coinext-py is a member but its pyo3 dependency is OPTIONAL and gated behind the `python` feature, so
# the default `cargo test` builds it as an (empty) lib without linking libpython. maturin enables
# the feature: `uvx maturin develop --manifest-path foundation/crates/coinext-py/Cargo.toml --features python`.
# The crates below pull heavy network/db deps and are kept out of the default build as stubs.
exclude = [
    "market-data/crates/coinext-network",
    "market-data/crates/coinext-adapters-binance",
    "operations-interface/crates/coinext-persistence",
    "market-data/crates/coinext-ingest",
    "execution-live/crates/coinext-exec-svc",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
rust-version = "1.95"
license = "MIT"
authors = ["Coinext"]

# Single source of dependency versions, inherited by member crates via `.workspace = true`.
[workspace.dependencies]
coinext-core = { path = "foundation/crates/coinext-core" }
coinext-model = { path = "foundation/crates/coinext-model" }
coinext-ports = { path = "foundation/crates/coinext-ports" }
coinext-cache = { path = "foundation/crates/coinext-cache" }
coinext-bus = { path = "operations-interface/crates/coinext-bus" }
coinext-indicators = { path = "strategy-research/crates/coinext-indicators" }
coinext-derivatives = { path = "analytics-optimization/crates/coinext-derivatives" }
coinext-risk-engine = { path = "risk-portfolio/crates/coinext-risk-engine" }
coinext-portfolio = { path = "risk-portfolio/crates/coinext-portfolio" }
coinext-data-engine = { path = "market-data/crates/coinext-data-engine" }
coinext-exec-engine = { path = "execution-live/crates/coinext-exec-engine" }
coinext-sim = { path = "backtesting-simulation/crates/coinext-sim" }
coinext-kernel = { path = "backtesting-simulation/crates/coinext-kernel" }
coinext-testkit = { path = "foundation/crates/coinext-testkit" }

rust_decimal = { version = "1", features = ["serde"] }
rust_decimal_macros = "1"
uuid = { version = "1", features = ["v7", "serde"] }
chrono = { version = "0.4", features = ["serde"] }
thiserror = "2"
serde = { version = "1", features = ["derive", "rc"] }
serde_json = "1"
fnv = "1"
async-trait = "0.1"
tokio = { version = "1", default-features = false, features = ["sync", "macros", "rt"] }

[profile.release]
lto = "thin"
codegen-units = 1
# NOTE: do NOT set `panic = "abort"` here. The maturin --release wheel inherits this profile, and
# under panic=abort a Rust panic inside the PyO3 bridge raises SIGABRT and kills the whole Python
# interpreter instead of surfacing as a catchable Python exception. Keep the default (unwind) so
# pyo3 can translate panics into Python exceptions.
